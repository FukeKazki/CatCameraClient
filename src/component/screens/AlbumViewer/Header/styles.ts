import { StyleSheet } from 'react-native'
import { componentBaseColor, textColor } from '~/utils/colors'

export default StyleSheet.create({
	container: {
		backgroundColor: componentBaseColor
	},
	title: {
		color: textColor
	}
})